<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.practice.account.dao.AccountDao">

    <insert id="createUser" parameterType="com.practice.account.vo.AccountVo">
        INSERT INTO USER_BASIC
        (  USER_ID  , USER_NAME , PASSWORD,  EMAIL
        ,  AGE      , ROLE )
        VALUES
        (  #{userId}, #{username}, #{password}, #{email}
        ,  #{age}   , #{role}
        )
    </insert>

    <select id="selectUserId" parameterType="java.lang.String" resultType="com.practice.account.vo.AccountVo">

        SELECT USER_ID
             , USER_NAME
             , PASSWORD
             , EMAIL
             , AGE
             , ROLE
          FROM USER_BASIC
         WHERE USER_ID = #{userId}

    </select>

</mapper>